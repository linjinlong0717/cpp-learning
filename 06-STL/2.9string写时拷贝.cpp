写时拷贝
一、定义
写时拷贝（Copy - On - Write） 是 std::string（以及其他一些容器）在底层实现中的一种内存优化策略。
核心思想：在拷贝一个字符串对象时，不立即复制其内部的字符数据，而是让多个对象共享同一份数据。
只有当某个对象需要修改其内容时，才真正执行一次深拷贝，为自己创建一份独立的数据副本。

二、核心原理
1. 共享机制
多个 string 对象的指针成员，指向同一块堆内存地址。
在该内存的头部（或旁边），存储一个引用计数（Reference Count），记录当前有多少个对象共享这块内存。

2. 读写行为
读取（不修改）：直接通过 共享指针 读取数据，引用计数不变。
修改（触发拷贝）：修改前检查引用计数：
如果计数 > 1（说明是共享状态），则申请新内存，复制原数据，在新内存上修改，然后将当前对象的指针指向新地址，并将旧内存的计数减 1。
如果计数 == 1（说明是独占状态），则直接修改，无需拷贝。

3. 析构行为
每个对象死亡时，析构函数一定执行。
但析构函数内部会判断引用计数：
将计数减 1 后，若结果为 0（说明自己是最后一个使用者），则执行 delete[] 释放内存。
若结果 大于 0（说明还有别人在用），则只减计数，不释放内存。

三、优点
1.减少不必要的深拷贝开销：在频繁拷贝但很少修改的场景下（如只读传递、函数传参），避免了昂贵的内存分配和字符复制。
2.节省内存：多份拷贝共享同一块内存，而不是每份拷贝都占据独立空间。
3.对程序员透明：使用时语法与普通 string 完全一致，开发者无需关心底层是否共享。

四、缺点（为什么现代 C++ 抛弃了它）
1.多线程环境下的性能问题：引用计数的增减操作需要是线程安全的，通常依赖原子操作（std::atomic），这会引入额外的同步开销。在高并发场景下，这种开销可能比深拷贝还大。
2.实现复杂，易出错：内存布局需要额外管理引用计数，涉及 operator[]、c_str()、迭代器等细节，容易出现隐蔽的 bug。
3.与 C++11 移动语义冲突：C++11 引入了移动语义（std::move），可以高效地转移资源所有权，COW 的“共享”策略在这种场景下失去了优势。

五、现状
已经基本被废弃。

现代 C++ 主流实现：
短字符串（通常 ≤ 15 字节）直接存储在栈上（SSO），避免堆内存分配。
长字符串使用深拷贝，配合移动语义，实现性能与安全的平衡。

六、你的核心记忆
“写时拷贝 = 共享内存 + 引用计数 + 读时共享 + 写时分家 + 最后离开者负责释放。”