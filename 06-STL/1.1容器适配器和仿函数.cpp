1. 容器适配器（Container Adapter）—— 它是“容器”的“皮肤”或“变形器”
一句话定义：容器适配器本身不是独立的容器，而是一个“限制器”。它把现有的容器（vector、deque、list）包起来，
强行屏蔽掉大部分功能，只保留特定的几个接口，从而实现特定的数据结构（栈、队列）

重点：适配器不支持迭代器（不能用 begin() 或 end() 去遍历它），因为它不让你看到内部的数据结构

2. 仿函数（Functor）—— 它是可以“像函数一样调用”的对象
一句话定义：仿函数是“重载了括号运算符 operator()”的类或结构体。它的对象可以像普通函数一样被调用 对象名(参数)

底层原理：
相比普通函数指针，仿函数是类对象，编译器能够对它进行内联展开，没有额外的指针跳转开销，性能更高。

例：
// 定义一个仿函数类（重载 operator()）
struct Add {
    int operator()(int a, int b) // 这就是仿函数的核心
    {                               
        return a + b;
    }
};

int main() 
{
    Add addObj;                // 创建仿函数对象
    int result = addObj(3, 5); // 像调用函数一样调用它，输出 8
    return 0;
}