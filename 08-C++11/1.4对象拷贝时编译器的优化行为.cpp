对象拷贝时编译器的优化行为
一、概念说明
RVO / NRVO 是两种不同场景的返回值优化，统称为返回值优化，是 C++11 编译器的一种编译期优化手段
// 注意：C++11 标准下二者都不是语法强制规则，只是可选编译器优化策略，关闭优化选项后该效果会消失

RVO：返回值优化（Return Value Optimization），用于函数返回匿名临时对象场景
NRVO：具名返回值优化（Named Return Value Optimization），用于函数返回有名称的局部对象场景

二、没有优化时（原始执行流程）
A fun()
{
	A a;
	return a;
}
A obj = fun();

1.在函数栈内创建局部对象 a
2.使用 a 拷贝构造临时对象
3.使用临时对象，拷贝构造外部对象 obj
4.依次析构临时对象、局部对象 a
//一共触发2 次拷贝构造，效率很低

开启优化后
编译器直接在外部变量 obj 的内存地址上构造对象，全程无额外拷贝，省去两次拷贝构造，大幅降低开销。

A fun()
{
	return A();
}
A obj = fun();

无优化：构造临时对象 → 拷贝构造 obj
优化后：直接在 obj 位置构造对象，不产生临时对象

// 总结：对象拷贝优化主要指 RVO、NRVO 返回值优化。函数返回对象并直接初始化外部变量时，编译器直接在外部接收对象的内存上构造实例，
// 省去中间临时对象以及拷贝构造过程，减少对象拷贝开销。C++11 中该行为只是可选编译器优化，不受语法强制约束。
