一、什么是迭代器失效
迭代器本质是一个指向容器内部元素的“指针”（或封装指针的对象）。当容器内部的内存布局或元素位置发生变化时，
这个“指针”指向的地址不再有效，或者不再指向原来的元素，这就是迭代器失效。

二、失效的两大根本原因
1.内存重分配（扩容）：vector / string 在插入元素超出容量时，会重新申请一块更大的新内存，把旧元素搬过去，释放旧内存。
之前指向旧内存的迭代器直接变成“野指针”。

2.元素位置移动（挪位 / 删除）：在 vector 中间插入或删除元素，会导致该位置之后的所有元素整体向后 / 向前移动，
指向这些元素的迭代器虽然没变成野指针，但已经指向了错误的位置（元素变了）。

三、
只要对容器执行了插入（insert）或删除（erase），如果后续代码还要用到这个位置的迭代器，
下一行代码立刻从容器里重新获取一次迭代器的值(重置)