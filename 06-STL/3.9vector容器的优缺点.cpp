//结论：绝大多数情况下，优先使用 vector。只有当你在容器中间（非两端）进行极其频繁的插入和删除，
且对迭代器稳定性有硬性要求时，才考虑 list

一、1.底层内存布局
vector：连续内存块，元素一个挨一个存储

2.vector优缺点
优点：
·极致的随机访问速度：支持下标[]直接寻址，时间复杂度O(1)
因为CPU只需一次算术运算即可定位内存地址

·完美的缓存命中率：遍历元素时，内存地址连续，CPU会预加载
后续数据到高速缓存，遍历速度极快

·内存开销极小：除了预留的备用容量外，几乎没有额外内存浪费

缺点：
·中间/头部插入删除极慢：在非队尾部位置操作，需要移动该位置之后的所有元素，
时间复杂度O(n)。在头部插入100万个元素，代价是灾难性的

·迭代器失效问题严重：插入元素导致扩容(重新分配内存)时，所有迭代器、指针和引用
全部失效；即使是尾部插入，也可能因扩容而失效

·内存峰值翻倍：扩容时(通常按1.5倍或2倍增长)，系统会同时保留旧内存和新内存，
在存储 大对象 时可能导致内存不足