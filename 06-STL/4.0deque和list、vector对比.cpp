vector / list / deque 完整对比
一、底层存储结构
vector：一整块连续内存（动态扩容）

list：分散的独立节点，通过指针前后相连

deque：多块连续小内存（缓冲区），由中控器（指针数组）串联

二、随机访问能力
vector：O(1) 极速。原理：首地址 + i * sizeof(T)
list：O(n) 不支持[]。原理：必须从头遍历
deque：O(1) 较快。原理：先找中控器，再在块内偏移（比 vector 多一次寻址）

三、插入与删除效率
只要不在头部操作，且在尾部操作前你能用 reserve 预分配空间，vector 的速度最快。

只要涉及头部和尾部频繁插入删除，deque 是最优选择——稳定且高速。

中间操作 list 在理论上 O(1) 最快，但前提是你已经持有了指向该位置的迭代器。
否则遍历查找位置的成本（O(n)）加上缓存不命中，会让它比 deque 慢得多。实战中慎用 list。

四、迭代器失效规则
操作：插入元素
vector：扩容则全部失效
list：完全不失效
deque：全部失效（中控器可能扩容）

操作：删除元素
vector：被删元素之后全部失效
list：仅被删元素失效
deque：全部失效（中控器结构变动）

操作：尾部插入 push_back
vector：扩容则全失效
list：安全
deque：全部失效

五、内存占用
vector：极紧凑，除 capacity 预留外无额外指针。额外开销：可能浪费少量 capacity - size 空间
list：极大浪费，每个节点多存两个指针（prev / next）。额外开销：存一个 int（4 字节）实际占用约 24 字节
deque：中等，中控器（指针数组） + 缓冲区内存。额外开销：比 vector 多中控器开销，但远小于 list

六、实战选择指南
场景：只在尾部插入 / 删除，需要极速随机访问
最优选择：vector
原因：缓存命中率最高，速度最快（默认首选）

场景：在头部和尾部频繁插入 / 删除，且需要随机访问
最优选择：deque
原因：两端操作 O(1)，内存相对紧凑

场景：需要在中间任意位置频繁插入 / 删除，且不需要随机访问
最优选择：list
原因：中间插入删除 O(1)（改指针），但仅在已知迭代器位置时成立

场景：绝大多数情况（不确定使用模式）
最优选择：vector
原因：大多场景 vector 足够快，且内存最紧凑