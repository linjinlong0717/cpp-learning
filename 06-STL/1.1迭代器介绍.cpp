一、迭代器的概念与本质
迭代器是C++ STL体系中核心的组件，被称为容器和算法之间的粘合剂。
STL中的算法（如遍历、查找、排序）不针对某一个具体容器编写，而是通过迭代器统一操作所有容器，极大提升了代码通用性。

迭代器的核心设计思想：提供一种统一的方式，依序访问容器中的每一个元素，
不需要暴露容器的底层存储结构，完美体现了封装的特性。

从本质上来说，迭代器并非指针，而是STL封装的类对象；
 在初学阶段，可以将迭代器近似理解为智能指针，支持解引用、自增、自减等类似指针的操作，使用逻辑和指针高度相似。

同时每个STL容器都拥有专属的迭代器，适配自身底层结构，不同容器的迭代器能力不同，无法通用。

二、迭代器核心基础用法
所有STL容器的迭代器都具备两个核心接口，是遍历容器的基础：
  begin()：返回迭代器，指向容器的第一个有效元素
  end()：返回迭代器，指向容器末尾元素的下一个位置（哨兵位），该位置无有效数据，不支持解引用取值
通用遍历规则：所有容器迭代器遍历统一使用 迭代器 != end() 判断结束，不建议使用大于、小于比较，保证适配所有类型迭代器。

三、迭代器的分类（核心两类）
1. 双向迭代器
功能特点：支持元素读写操作，可向前、向后双向遍历容器元素，灵活性中等。
支持运算：仅支持迭代器自增 ++、自减 --
能力限制：不支持跳跃访问、下标取值、迭代器大小比较运算
对应容器：list、map、set（链表结构、红黑树结构容器）

2. 随机访问迭代器
功能特点：功能最强的迭代器，支持元素读写操作，可自由跳跃式访问容器中任意位置的数据，遍历效率最高。
支持运算：自增 ++、自减 --、下标[n] 取值、 + n / -n 跳跃访问、迭代器大小比较（<、 <= 、>、 >= ）
对应容器：vector、string、array、deque（连续内存结构容器）

四、迭代器延伸类型（基础了解）
除核心两类迭代器外，STL还提供适配不同场景的迭代器，满足多样化开发需求：
  普通迭代器（iterator）：可读取、修改容器元素，是最常用的迭代器类型。
  常量迭代器（const_iterator）：仅可读取元素，无法修改容器数据，适用于只读遍历场景，保护数据安全性。
  反向迭代器（reverse_iterator）：支持逆序遍历容器，通过 rbegin()、rend() 实现从容器末尾到开头的遍历。

五、迭代器核心特性总结
1. 通用性：统一所有容器的遍历方式，适配STL通用算法，解耦容器与算法。
2. 封装性：无需关注容器底层存储结构，仅通过迭代器即可操作元素。
3. 差异性：不同容器迭代器能力不同，连续内存容器迭代器能力最强，链式、树状容器迭代器能力受限。